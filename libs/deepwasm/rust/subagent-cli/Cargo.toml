[package]
name = "subagent-cli"
version = "0.1.0"
edition = "2021"
description = "CLI tool for the subagent WASIX execution engine"

[[bin]]
name = "subagent"
path = "src/main.rs"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
